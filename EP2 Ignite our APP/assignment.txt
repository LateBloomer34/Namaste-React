1 - what is NPM-    NPM manage the node packages. menas install or unindtall the packages of node.
to install  NPM 
NPM init
ex - to install package :- parcel .
npm install parcel
to install parcel as a devloper 
npm install -D parcel

2- what is parcel / webpack ? why we do need it.
parcel is a bundler, bundler is a tool to combine diff- diff (munltiple) packages. every packages has a unique functionality.

to add functionaly in app we use the parcel,, some of functionality are:-
HMR - hot module rendering
tree shaking  (revode unwanted code)
optimize the code using diff algo ,
image optimization.
remove white space from code.
allocate unique port number for diff app.
remove console.log and short the variable name. 
catching for fast rendering
enable https 
add functionality in older browser using pollyfills 
code compatible with other version using babel
minify the code 
bundling.

3- What is `.parcel-cache` 
it stores the cache files of the app(temp files), which helps to reload app faster. makes app faster

4- What is `npx`?
node package executer.
it create the unique devlopment envirement server for the app to run packages.

5-  What is difference between `dependencies` vs `devDependencies` 
dependencies are the packages project needed.
devDependencies are the packages using while the devlopment of project.

6 - What is Tree Shaking? 
remove unwanted or extra code which is not require for app , is called tree shaking. 
 
7- What is Hot Module Replacement?
automatic reload while update in app.

8-List down your favourite 5 superpowers of Parcel  - 
HMR
tree shaking
catching
image and code optimization.
bundling
enable https

9-What is `.gitignore`? What should we add and not add into it?
to remain file untracked in git.
.gitignore store the info about files which are not needed to track in the git.
-- large size file which can generted by server are not needed to put in .gitignore.

10-  What is the difference between `package.json` and `package-lock.json`
package.json -  stores the info about list of dependencies.
package-lock.json - info about exact version of the dependencies. (no need to put in .gitignore)

11- Why should I not modify `package-lock.json`? 
because it stores the exact vesion of the dependencies

12 - What is `node_modules` ? Is it a good idea to push that on git? 
node module is the directory , where all packages(dependencied) use in app are stores .
always put in git ignore , because we can generte these node module files again, with the help of packge-lock.json

13 - What is the `dist` folder?
output directory where the production-ready build of your application is stored.
inside dist folder : 
minified css and js code
index.html :- file, entry point of the project
static asset -  stores file and image use in app dev

14 - What is `browserlists`.
browserlist tells the info about the brower compatibilty with the app. 

15 - ^  caret and ~  tilda notation . 
^ It automatically updates both minor and patch updates.
~ The tilde (~) notation is employed to match the latest patch version while freezing the major and minor versions.

16 - diff Script types in html (MDN Docs) 
i- <script></script>
ii-  <script src= "file name"></script>
iii- <script type="module" src=""></script> -  it helps to export module from depencencies.
